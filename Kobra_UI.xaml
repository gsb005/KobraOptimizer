<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KobraOptimizer v1.7.0"
        Height="780"
        Width="1320"
        MinHeight="720"
        MaxHeight="860"
        MinWidth="1220"
        MaxWidth="1540"
        Background="#0B0B0B"
        FontSize="13"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        SizeToContent="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#00FF41"/>
        <SolidColorBrush x:Key="AccentDarkBrush" Color="#00B530"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#131313"/>
        <SolidColorBrush x:Key="CardBrush" Color="#151515"/>
        <SolidColorBrush x:Key="CardBrushAlt" Color="#101010"/>
        <SolidColorBrush x:Key="BorderBrushDark" Color="#2B2B2B"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#CFCFCF"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="#101010"/>
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="Background" Value="#101010"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="KobraBaseButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#232323"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00FF41" BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2E2E2E"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#00CC35"/>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF41" BlurRadius="24" ShadowDepth="0" Opacity="0.45"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1C1C1C"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#00FF41"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KobraAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource KobraBaseButtonStyle}">
            <Setter Property="Background" Value="#00FF41"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#00FF41"/>
        </Style>

        <Style x:Key="SidebarNavButtonStyle" TargetType="Button" BasedOn="{StaticResource KobraBaseButtonStyle}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="BorderBrush" Value="#1F1F1F"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="KobraToolButtonStyle" TargetType="Button" BasedOn="{StaticResource KobraBaseButtonStyle}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border Margin="0,10,0,0"
                                    CornerRadius="12"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#00FF41" BlurRadius="14" ShadowDepth="0" Opacity="0.2"/>
                                </Border.Effect>
                            </Border>
                            <Border Margin="12,0,0,0"
                                    Padding="8,0"
                                    Background="#0B0B0B"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top">
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                            </Border>
                            <ContentPresenter Margin="12,28,12,12"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="Padding" Value="2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#00CC35"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E1E1E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#101010"/>
            <Setter Property="Foreground" Value="#00FF41"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="KobraComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#2F2F2F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="18,6,0,0"/>
            <Setter Property="Padding" Value="6,2"/>
        </Style>

        <Style x:Key="KobraComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="Padding" Value="10,7"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#121212" BorderBrush="#252525" BorderThickness="0,0,1,0">
            <DockPanel Margin="16">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="KOBRA" Foreground="#00FF41" FontSize="34" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="OPTIMIZER" FontSize="13" HorizontalAlignment="Center" Margin="0,-4,0,12"/>
                    <Border Width="156" Height="156" Background="#0F0F0F" CornerRadius="12" BorderBrush="#262626" BorderThickness="1" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <Grid>
                            <Ellipse Width="128" Height="128" Fill="#141414" Stroke="#00AA2B" StrokeThickness="1" Opacity="0.8">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#00FF41" BlurRadius="22" ShadowDepth="0" Opacity="0.45"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <Image Name="KobraLogo" Width="128" Height="128" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel DockPanel.Dock="Top">
                    <Button Name="BtnNavDashboard" Content="Dashboard" Style="{StaticResource SidebarNavButtonStyle}"/>
                    <Button Name="BtnNavAnalyze" Content="Analyze" Style="{StaticResource SidebarNavButtonStyle}"/>
                    <Button Name="BtnNavResults" Content="Results" Style="{StaticResource SidebarNavButtonStyle}"/>
                    <Button Name="BtnNavTools" Content="Tools" Style="{StaticResource SidebarNavButtonStyle}"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,14,0,10">
                        <StackPanel>
                            <TextBlock Text="v1.7.0 shell pass" Foreground="#00FF41" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBlock Text="• Dashboard + Analyze + Results + Tools navigation" TextWrapping="Wrap" FontSize="12" Foreground="#CFCFCF"/>
                            <TextBlock Text="• Keeps the existing PowerShell backend intact" TextWrapping="Wrap" FontSize="12" Foreground="#CFCFCF" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Button Name="BtnExit" Content="EXIT" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition x:Name="LogRow" Height="180"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Menu Grid.Row="0">
                <MenuItem Header="_File">
                    <MenuItem Header="_Analyze" Name="MenuFileAnalyze"/>
                    <MenuItem Header="_Execute Selected" Name="MenuFileExecute"/>
                    <Separator/>
                    <MenuItem Header="_Quick Shed" Name="MenuFileQuickShed"/>
                    <MenuItem Header="_Create Backup" Name="MenuFileBackup"/>
                    <Separator/>
                    <MenuItem Header="E_xit" Name="MenuFileExit"/>
                </MenuItem>
                <MenuItem Header="_Tools">
                    <MenuItem Header="Open _Logs" Name="MenuToolsLogs"/>
                    <MenuItem Header="Open _Manifests" Name="MenuToolsManifests"/>
                </MenuItem>
                <MenuItem Header="_Help">
                    <MenuItem Header="_Donate" Name="MenuHelpSupport"/>
                    <MenuItem Header="_Disclaimer" Name="MenuHelpDisclaimer"/>
                    <Separator/>
                    <MenuItem Header="_About Me" Name="MenuHelpAbout"/>
                </MenuItem>
            </Menu>

            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="18" Margin="0,12,0,10">
                <Grid>
                    <Grid Name="ViewDashboard" Visibility="Visible">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Border Style="{StaticResource CardBorderStyle}" Background="#101010" Margin="0,0,0,14">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Margin="2,0,18,0">
                                            <TextBlock Text="Ready to optimize your PC" Foreground="#00FF41" FontSize="28" FontWeight="Bold"/>
                                            <TextBlock Text="Analyze junk files, review category-by-category results, and run cleanup with backup-aware tools." TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,8,0,16" FontSize="14"/>
                                            <WrapPanel>
                                                <Button Name="BtnDashboardAnalyze" Content="Analyze Now" Width="150" Style="{StaticResource KobraAccentButtonStyle}" Margin="0,0,10,0"/>
                                                <Button Name="BtnDashboardQuickShed" Content="Quick Shed" Width="140" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,10,0"/>
                                                <Button Name="BtnDashboardBackup" Content="Create Backup" Width="150" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,10,0"/>
                                                <Button Name="BtnDashboardViewResults" Content="Open Results" Width="140" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,0,0"/>
                                            </WrapPanel>
                                        </StackPanel>
                                        <Border Grid.Column="1" Width="220" Background="#151515" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="12" Padding="16">
                                            <StackPanel>
                                                <TextBlock Text="Safety first" Foreground="#00FF41" FontWeight="Bold" FontSize="16"/>
                                                <TextBlock Name="TxtDashSafety" Text="No backup bundle yet. Create one before deep system changes." TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>

                                <UniformGrid Columns="4" Margin="0,0,0,14">
                                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="LAST SCAN" Foreground="#8D8D8D" FontSize="12"/>
                                            <TextBlock Name="TxtDashLastScan" Text="No scan yet" Foreground="#00FF41" FontSize="22" FontWeight="Bold" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="RECLAIMABLE" Foreground="#8D8D8D" FontSize="12"/>
                                            <TextBlock Name="TxtDashReclaimable" Text="--" Foreground="#00FF41" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="RECORDS FOUND" Foreground="#8D8D8D" FontSize="12"/>
                                            <TextBlock Name="TxtDashRecords" Text="--" Foreground="#00FF41" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource CardBorderStyle}">
                                        <StackPanel>
                                            <TextBlock Text="CATEGORIES" Foreground="#8D8D8D" FontSize="12"/>
                                            <TextBlock Name="TxtDashCategories" Text="--" Foreground="#00FF41" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </UniformGrid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.25*"/>
                                        <ColumnDefinition Width="0.75*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="Recent activity" Foreground="#00FF41" FontSize="16" FontWeight="Bold"/>
                                            <TextBox Name="TxtRecentActivity" Margin="0,10,0,0" Background="#121212" Foreground="#D8D8D8" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="160"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Grid.Column="1">
                                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                                            <StackPanel>
                                                <TextBlock Text="Selections" Foreground="#00FF41" FontSize="16" FontWeight="Bold"/>
                                                <TextBlock Name="TxtDashSelections" Text="No cleanup categories selected yet." TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Style="{StaticResource CardBorderStyle}">
                                            <StackPanel>
                                                <TextBlock Text="What's new" Foreground="#00FF41" FontSize="16" FontWeight="Bold"/>
                                                <TextBlock Text="• Product-style navigation shell&#x0a;• Bigger Results experience&#x0a;• Dashboard cards and recent activity" TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,8,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid Name="ViewAnalyze" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.05*"/>
                                    <ColumnDefinition Width="0.95*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <GroupBox Header="Analyze your selections">
                                        <StackPanel>
                                            <TextBlock Name="TxtAnalyzeStage" Text="Ready to analyze" Foreground="#00FF41" FontSize="22" FontWeight="Bold"/>
                                            <TextBlock Name="TxtAnalyzeSubStage" Text="Select your cleanup categories, then run Analyze to build a reviewable results screen." Foreground="#CFCFCF" TextWrapping="Wrap" Margin="0,8,0,14"/>
                                            <WrapPanel>
                                                <Button Name="BtnAnalyze" Content="ANALYZE" Width="150" Style="{StaticResource KobraAccentButtonStyle}" Margin="0,0,10,0"/>
                                                <Button Name="BtnQuickShed" Content="QUICK SHED" Width="150" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,10,0"/>
                                                <Button Name="BtnCreateBackup" Content="CREATE BACKUP" Width="160" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,0,0"/>
                                            </WrapPanel>
                                        </StackPanel>
                                    </GroupBox>

                                    <GroupBox Header="System Cleanup">
                                        <StackPanel>
                                            <CheckBox Name="ChkUserTemp" Content="User Temp" IsChecked="True"/>
                                            <CheckBox Name="ChkSystemTemp" Content="System Temp" IsChecked="True"/>
                                            <CheckBox Name="ChkWinUpdate" Content="Windows Update Cache"/>
                                            <CheckBox Name="ChkThumbCache" Content="Thumbnail / Icon Cache" IsChecked="True"/>
                                            <CheckBox Name="ChkShaderCache" Content="DirectX Shader Cache"/>
                                            <CheckBox Name="ChkRecycleBin" Content="Recycle Bin" IsChecked="True"/>
                                            <TextBlock Text="Shader cache rebuilds on the next game launch, so the first launch after cleanup can be slower." TextWrapping="Wrap" Foreground="#CFCFCF" FontSize="12" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </GroupBox>

                                    <GroupBox Header="Browser Cleanup">
                                        <StackPanel>
                                            <CheckBox Name="ChkChrome" Content="Google Chrome"/>
                                            <CheckBox Name="ChkEdge" Content="Microsoft Edge"/>
                                            <CheckBox Name="ChkFirefox" Content="Firefox"/>
                                            <CheckBox Name="ChkBrowserCookies" Content="Also clean browser cookies"/>
                                            <TextBlock Text="Saved passwords and bookmarks stay intact. Cookie cleaning may sign you out of websites." TextWrapping="Wrap" Foreground="#CFCFCF" FontSize="12" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <GroupBox Header="System Tuning">
                                        <StackPanel>
                                            <CheckBox Name="ChkRestorePoint" Content="Create Restore Point" IsChecked="True"/>
                                            <CheckBox Name="ChkRegistry" Content="TLS Hardening"/>
                                            <CheckBox Name="ChkNetwork" Content="Network TCP Strike"/>
                                            <CheckBox Name="ChkDnsFlush" Content="Flush DNS Cache"/>
                                            <CheckBox Name="ChkDnsProfile" Content="Apply DNS Profile"/>
                                            <ComboBox Name="CmbDnsProvider" Style="{StaticResource KobraComboBoxStyle}" SelectedIndex="0" IsEnabled="False" Opacity="0.72">
                                                <ComboBoxItem Style="{StaticResource KobraComboBoxItemStyle}" Content="Cloudflare (1.1.1.1 / 1.0.0.1)"/>
                                                <ComboBoxItem Style="{StaticResource KobraComboBoxItemStyle}" Content="Google (8.8.8.8 / 8.8.4.4)"/>
                                                <ComboBoxItem Style="{StaticResource KobraComboBoxItemStyle}" Content="Automatic / DHCP"/>
                                            </ComboBox>
                                            <CheckBox Name="ChkHPDebloat" Content="HP Telemetry Trim"/>
                                            <TextBlock Text="Analyze covers cleanup data first. System-wide tweaks still follow the confirmation flow before execution." TextWrapping="Wrap" Foreground="#CFCFCF" FontSize="12" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </GroupBox>

                                    <GroupBox Header="Analyze workflow">
                                        <StackPanel>
                                            <TextBlock Text="1. Choose categories" Foreground="#00FF41" FontWeight="Bold"/>
                                            <TextBlock Text="2. Run Analyze" Margin="0,6,0,0"/>
                                            <TextBlock Text="3. Review Results" Margin="0,6,0,0"/>
                                            <TextBlock Text="4. Clean Selected" Margin="0,6,0,0"/>
                                            <TextBlock Text="Logs and manifests remain available for advanced review below." Foreground="#CFCFCF" TextWrapping="Wrap" Margin="0,12,0,0" FontSize="12"/>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </Grid>

                    <Grid Name="ViewResults" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*"/>
                            <ColumnDefinition Width="0.85*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,14,0">
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Name="TxtResultsHeadline" Text="Analyze to see removable records" Foreground="#00FF41" FontSize="28" FontWeight="Bold"/>
                                    <TextBlock Name="TxtResultsSubHeadline" Text="Run Analyze to build your category-by-category review screen." Foreground="#CFCFCF" TextWrapping="Wrap" Margin="0,8,0,0" FontSize="14"/>
                                </StackPanel>
                            </Border>
                            <GroupBox Header="Review your results">
                                <ListView Name="ResultsList" Height="420" IsHitTestVisible="False">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="230"/>
                                            <GridViewColumn Header="Items" DisplayMemberBinding="{Binding ItemsText}" Width="110"/>
                                            <GridViewColumn Header="Size" DisplayMemberBinding="{Binding SizeText}" Width="120"/>
                                            <GridViewColumn Header="Notes" DisplayMemberBinding="{Binding Note}" Width="260"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </GroupBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="Ready to clean" Foreground="#00FF41" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Name="TxtSelectedCategoryCount" Text="0 categories selected" Margin="0,10,0,0" FontSize="14"/>
                                    <TextBlock Name="TxtSelectedRecordCount" Text="0 records selected" Margin="0,6,0,0" FontSize="14"/>
                                    <TextBlock Name="TxtSelectedBytes" Text="0.00 MB selected" Margin="0,6,0,0" FontSize="14"/>
                                    <TextBlock Name="TxtSelectedWarnings" Text="Review the results, then run Clean Selected when you are ready." TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                            <Button Name="BtnRunSelected" Content="CLEAN SELECTED" Style="{StaticResource KobraAccentButtonStyle}" Height="46"/>
                            <Button Name="BtnOpenManifests" Content="OPEN MANIFESTS" Style="{StaticResource KobraBaseButtonStyle}" Width="170"/>

                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,12,0,0">
                                <StackPanel>
                                    <TextBlock Text="Results notes" Foreground="#00FF41" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="Use the Analyze screen checkboxes to control what will be cleaned. This results view reflects those selections and the latest manifest." TextWrapping="Wrap" Foreground="#CFCFCF" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Name="ViewTools" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.12*"/>
                                    <ColumnDefinition Width="0.88*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <GroupBox Header="Startup Manager">
                                        <StackPanel>
                                            <CheckBox Name="ChkStartupShowMicrosoft" Content="Show Microsoft entries"/>
                                            <ListView Name="StartupList" Height="260" SelectionMode="Extended" Margin="0,10,0,0">
                                                <ListView.View>
                                                    <GridView>
                                                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                                                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="80"/>
                                                        <GridViewColumn Header="Location" DisplayMemberBinding="{Binding Location}" Width="110"/>
                                                        <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Kind}" Width="90"/>
                                                        <GridViewColumn Header="Command" DisplayMemberBinding="{Binding Command}" Width="280"/>
                                                    </GridView>
                                                </ListView.View>
                                            </ListView>
                                            <WrapPanel Margin="0,10,0,0">
                                                <Button Name="BtnStartupRefresh" Content="Refresh" Width="90" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,8,0"/>
                                                <Button Name="BtnStartupDisable" Content="Disable selected" Width="136" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,8,0"/>
                                                <Button Name="BtnStartupEnable" Content="Re-enable selected" Width="150" Style="{StaticResource KobraBaseButtonStyle}" Margin="0,0,0,0"/>
                                            </WrapPanel>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <GroupBox Header="Windows Tools">
                                        <UniformGrid Columns="2" Margin="0,4,0,0">
                                            <Button Name="BtnWindowsUpdate" Content="Check for updates" Margin="0,0,8,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsUpdateSettings" Content="Update settings" Margin="0,0,0,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsStorage" Content="Storage" Margin="0,0,8,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsApps" Content="Apps &amp; Features" Margin="0,0,0,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsStartupSettings" Content="Startup Apps" Margin="0,0,8,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsGameMode" Content="Game Mode" Margin="0,0,0,8" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsGraphics" Content="Graphics settings" Margin="0,0,8,0" Style="{StaticResource KobraToolButtonStyle}"/>
                                            <Button Name="BtnWindowsPower" Content="Power &amp; sleep" Margin="0,0,0,0" Style="{StaticResource KobraToolButtonStyle}"/>
                                        </UniformGrid>
                                    </GroupBox>

                                    <GroupBox Header="Maintenance">
                                        <StackPanel>
                                            <Button Name="BtnOpenLogs" Content="OPEN LOGS" Style="{StaticResource KobraBaseButtonStyle}" Width="150"/>
                                            <TextBlock Text="Use logs and manifests for advanced review, troubleshooting, and issue reporting." Foreground="#CFCFCF" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="2" Margin="0,0,0,8" Background="#101010" BorderBrush="#1E1E1E" BorderThickness="1" CornerRadius="10" Padding="12,6">
                <DockPanel>
                    <TextBlock Text="System log" Foreground="#00FF41" FontSize="15" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                    <Button Name="BtnToggleLog" Content="Hide log" Style="{StaticResource KobraToolButtonStyle}" Width="90" Margin="0,0,0,0" DockPanel.Dock="Right"/>
                    <TextBlock Text="Advanced details stay here while the main views stay cleaner" Foreground="#8D8D8D" FontSize="11" HorizontalAlignment="Right" Margin="8,0,0,0"/>
                </DockPanel>
            </Border>

            <Border Grid.Row="3" Background="#151515" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="12" Padding="10">
                <TextBox Name="StatusTextBox"
                         Background="#151515"
                         Foreground="#D8D8D8"
                         BorderThickness="0"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         FontSize="13"/>
            </Border>

            <StackPanel Grid.Row="4" Margin="0,0,0,0">
                <ProgressBar Name="ProgBar" Height="14" Minimum="0" Maximum="100" Background="#232323" Foreground="#00FF41"/>
                <DockPanel Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Name="BtnDonate" Content="Donate" Style="{StaticResource KobraToolButtonStyle}" Width="140" Margin="0,0,8,0"/>
                        <Button Name="BtnDisclaimer" Content="Disclaimer" Style="{StaticResource KobraToolButtonStyle}" Width="110" Margin="0,0,8,0"/>
                        <Button Name="BtnAboutMe" Content="About Me" Style="{StaticResource KobraToolButtonStyle}" Width="120" Margin="0,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="KobraOptimizer v1.7.0  |  Dashboard shell + existing PowerShell engine" Foreground="#8D8D8D" FontSize="11" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                </DockPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
